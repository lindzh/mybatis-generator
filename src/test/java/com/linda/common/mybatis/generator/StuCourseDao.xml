<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--  AutoGenerated by lindzh  2015年11月15日 10:50 -->
<mapper namespace="com.linda.common.mybatis.generator.StuCourseDao">

	<resultMap id="StuCourseResultMap" type="com.linda.common.mybatis.generator.StuCourse">
		<id property="id" column="id"/>
		<result property="stuId" column="stu_id"/>
		<result property="courseId" column="course_id"/>
		<result property="addTime" column="add_time"/>
		<result property="score" column="score"/>
	</resultMap>
	
	<insert id="addStuCourse" useGeneratedKeys="true" keyProperty="id" parameterType="com.linda.common.mybatis.generator.StuCourse">
		insert into stu_course(stu_id,course_id,add_time,score)
		 values(#{obj.stuId},#{obj.courseId},#{obj.addTime},#{obj.score})
	</insert>
	
	<select id="getById" resultMap="StuCourseResultMap">
		select * from stu_course where id=#{id}
	</select>
	
	<update id="updateById" parameterType="com.linda.common.mybatis.generator.StuCourse">
		update stu_course set stu_id=#{obj.stuId},course_id=#{obj.courseId},add_time=#{obj.addTime},score=#{obj.score}
		where id=#{obj.id}
	</update>
	
	<delete id="deleteById">
		delete from stu_course where id=#{id}
	</delete>
	
	<select id="getByUserAndCourse" resultMap="StuCourseResultMap">
		select * from stu_course where stu_id=#{stuId} and course_id=#{courseId}
	</select>
	
	<update id="updateByUserAndCourse" parameterType="com.linda.common.mybatis.generator.StuCourse">
		update stu_course set add_time=#{obj.addTime},score=#{obj.score}
		where stu_id=#{obj.stuId} and course_id=#{obj.courseId}
	</update>
	
	<delete id="deleteByUserAndCourse">
		delete from stu_course where stu_id=#{stuId} and course_id=#{courseId}
	</delete>
	

	<select id="getListByCountAndTime" resultMap="StuCourseResultMap">
		select * from stu_course where
		course_id=#{courseId} and add_time=#{addTime}
		order by id desc limit #{limit} offset #{offset}
	</select>
	
	<select id="getCountByCountAndTime" resultType="long">
		select count(*) from stu_course where 
		course_id=#{courseId} and add_time=#{addTime}
	</select>
	
	
</mapper>