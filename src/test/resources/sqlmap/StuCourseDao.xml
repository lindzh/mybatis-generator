<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--  AutoGenerated by lindzh  2015年11月15日 12:09 -->
<mapper namespace="com.linda.common.mybatis.generator.dao.StuCourseDao">

	<resultMap id="StuCourseResultMap" type="com.linda.common.mybatis.generator.bean.StuCourse">
		<id property="id" column="id"/>
		<result property="stuId" column="stu_id"/>
		<result property="courseId" column="course_id"/>
		<result property="addTime" column="add_time"/>
		<result property="score" column="score"/>
	</resultMap>
	
	<insert id="addStuCourse" useGeneratedKeys="true" keyProperty="id" parameterType="com.linda.common.mybatis.generator.bean.StuCourse">
		insert into stu_course(stu_id,course_id,add_time,score)
		 values(#{stuId},#{courseId},#{addTime},#{score})
	</insert>
	
	<select id="getById" resultMap="StuCourseResultMap">
		select * from stu_course where id=#{id}
	</select>
	
	<update id="updateById" parameterType="com.linda.common.mybatis.generator.bean.StuCourse">
		update stu_course set stu_id=#{obj.stuId},course_id=#{obj.courseId},add_time=#{obj.addTime},score=#{obj.score}
		where id=#{obj.id}
	</update>
	
	<delete id="deleteById">
		delete from stu_course where id=#{id}
	</delete>
	
	<select id="getByUserAndCourse" resultMap="StuCourseResultMap">
		select * from stu_course where stu_id=#{stuId} and course_id=#{courseId}
	</select>
	
	<update id="updateByUserAndCourse" parameterType="com.linda.common.mybatis.generator.bean.StuCourse">
		update stu_course set add_time=#{obj.addTime},score=#{obj.score}
		where stu_id=#{obj.stuId} and course_id=#{obj.courseId}
	</update>
	
	<delete id="deleteByUserAndCourse">
		delete from stu_course where stu_id=#{stuId} and course_id=#{courseId}
	</delete>
	
	<select id="getListByCourseAndTime" resultMap="StuCourseResultMap">
        select * from stu_course where
        course_id=#{courseId} and add_time&gt;=#{startTime} and add_time&lt;=#{endTime}
        order by id desc limit #{limit} offset #{offset}
    </select>

    <select id="getCountByCourseAndTime" resultType="long">
        select count(*) from stu_course where 
        course_id=#{courseId} and add_time&gt;=#{startTime} and add_time&lt;=#{endTime}
    </select>
	
</mapper>