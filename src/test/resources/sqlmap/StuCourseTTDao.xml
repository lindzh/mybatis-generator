<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--  AutoGenerated by lindzh  2015年11月15日 12:19 -->
<mapper namespace="StuCourseTTDao">

	<resultMap id="StuCourseTTResultMap" type="StuCourseTT">
		<id property="id" column="id"/>
		<result property="stuId" column="stu_id"/>
		<result property="courseId" column="course_id"/>
	</resultMap>
	
	<insert id="addStuCourseTT" useGeneratedKeys="true" keyProperty="id" parameterType="StuCourseTT">
		insert into stu_course(stu_id,course_id)
		 values(#{stuId},#{courseId})
	</insert>
	
	<select id="getById" resultMap="StuCourseTTResultMap">
		select * from stu_course where id=#{id}
	</select>
	
	<update id="updateById" parameterType="StuCourseTT">
		update stu_course set stu_id=#{obj.stuId},course_id=#{obj.courseId}
		where id=#{obj.id}
	</update>
	
	<delete id="deleteById">
		delete from stu_course where id=#{id}
	</delete>
	
	<select id="getByUserAndCourse" resultMap="StuCourseTTResultMap">
		select * from stu_course where stu_id=#{stuId} and course_id=#{courseId}
	</select>
	
	
	<delete id="deleteByUserAndCourse">
		delete from stu_course where stu_id=#{stuId} and course_id=#{courseId}
	</delete>
	

	<select id="getListByCourseAndTime" resultMap="StuCourseTTResultMap">
		select * from stu_course where
		course_id=#{courseId}
		order by id desc limit #{limit} offset #{offset}
	</select>
	
	<select id="getCountByCourseAndTime" resultType="long">
		select count(*) from stu_course where 
		course_id=#{courseId}
	</select>
	
	
</mapper>